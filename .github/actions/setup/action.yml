name: Setup Node
description: Checkout repository, setup Node.js 22 with npm cache, and install dependencies

runs:
  using: composite
  steps:
    - uses: actions/setup-node@v4
      with:
        node-version: '22'
        cache: 'npm'

    - run: npm ci
      shell: bash
